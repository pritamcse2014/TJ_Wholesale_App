const API_BASE_URL = "https://wholesale.techjodo.xyz/api"; // Replace with your actual API base URL

export const API_URLS = {
    getVariations: `${API_BASE_URL}/variations`,
    register: `${API_BASE_URL}/register`,
    login: `${API_BASE_URL}/login`,
    // Add more endpoints as needed
};
